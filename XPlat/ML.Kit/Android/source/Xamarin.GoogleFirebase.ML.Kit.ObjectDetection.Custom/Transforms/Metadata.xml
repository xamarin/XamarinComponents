<?xml version="1.0" encoding="UTF-8"?>
<metadata>

    <!--
    TODO: possible problems! return type covariance/contravariance (needs investigation)
    
    CustomObjectDetectorOptions.Builder.Build()
    
    removing `build` with 
    renaming in Addditions
    -->    
    <remove-node
        path="/api/package[@name='com.google.mlkit.vision.objects.custom']/class[@name='CustomObjectDetectorOptions.Builder']/method[@name='build' and count(parameter)=0]"
        >        
    </remove-node>
    
    <!--
    TODO: possible problems! return type covariance/contravariance (needs investigation)
    
    ObjectDetectorOptions.Builder.enableClassification()
    
    removing `enableClassification` with 
    renaming in Addditions
    -->    
    <remove-node
        path="/api/package[@name='com.google.mlkit.vision.objects.custom']/class[@name='CustomObjectDetectorOptions.Builder']/method[@name='enableClassification' and count(parameter)=0]"
        >        
    </remove-node>
    
    <!--
    TODO: possible problems! return type covariance/contravariance (needs investigation)
    
    ObjectDetectorOptions.Builder.EnableMultipleObjects()
    
    removing `enableMultipleObjects` with 
    renaming in Addditions
    -->    
    <remove-node
        path="/api/package[@name='com.google.mlkit.vision.objects.custom']/class[@name='CustomObjectDetectorOptions.Builder']/method[@name='enableMultipleObjects' and count(parameter)=0]"
        >        
    </remove-node>

    <!--
    TODO: possible problems! return type covariance/contravariance (needs investigation)
    
    ObjectDetectorOptions.Builder.SetDetectorMode()
    
    removing `setDetectorMode` with
    renaming in Addditions
    -->    
    <remove-node
        path="/api/package[@name='com.google.mlkit.vision.objects.custom']/class[@name='CustomObjectDetectorOptions.Builder']/method[@name='setDetectorMode' and count(parameter)=1 and parameter[1][@type='int']]"
        >        
    </remove-node>
    
    
</metadata>
